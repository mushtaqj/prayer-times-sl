/**
 * Countdown constants
 */

// ============================================================================
// Timing
// ============================================================================

/** Default update interval in milliseconds (1 minute) */
export const DEFAULT_UPDATE_INTERVAL_MS = 60000

/** Fallback prayer duration in milliseconds (~6 hours between prayers) */
export const FALLBACK_PRAYER_DURATION_MS = 6 * 60 * 60 * 1000

// ============================================================================
// Progress Bounds
// ============================================================================

/** Minimum progress percentage */
export const MIN_PROGRESS = 0

/** Maximum progress percentage */
export const MAX_PROGRESS = 100
